{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "colcon build",
            "type": "shell",
            "command": "source ~/miniforge3/etc/profile.d/conda.sh && conda activate ros2_env && cd ${workspaceFolder}/ros2_ws && colcon build --symlink-install",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "echo": true,
                "reveal": "always",
                "panel": "shared"
            },
            "problemMatcher": []
        }
    ]
}
